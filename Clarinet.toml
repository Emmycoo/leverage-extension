[project]
name = "leverage-extension"
authors = ["Leverage Tech Team"]
description = "A decentralized health data management platform built on Stacks blockchain"
telemetry = false
requirements = []
[contracts.leverage-vault]
path = "contracts/leverage-vault.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
